CREATE OR REPLACE TRIGGER TG_POSITIVOS
BEFORE INSERT OR UPDATE
ON ITEMPEDIDO
FOR EACH ROW
BEGIN
    IF :NEW.QUANTIDADE <= 0 THEN
    RAISE_APPLICATION_ERROR(-20002, 'VALOR TEM QUE SER POSITIVO');
    END IF;
END;